<nav id="navbarExample" class="navbar navbar-expand-lg fixed-top navbar-light top-nav-collapse" aria-label="Main navigation">
        <div class="container">

            <!-- Image Logo -->
            <a class="navbar-brand logo-image" href="{{path('app_home')}}">SCA Demi-Fond</a> 

            <!-- Text Logo - Use this if you don't have a graphic logo -->
            <!-- <a class="navbar-brand logo-text" href="index.html">Zinc</a> -->

            <button class="navbar-toggler p-0 border-0" type="button" id="navbarSideCollapse" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="navbar-collapse offcanvas-collapse" id="navbarsExampleDefault">
                <ul class="navbar-nav ms-auto navbar-nav-scroll">
                    <li class="nav-item">
                        <a class="nav-link {% if current_menu is defined and current_menu == 'app_home' %}active{% endif %}" href="{{path('app_home')}}">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {% if current_menu is defined and current_menu == 'app_about' %}active{% endif %}" href="{{path('app_about')}}">Présentation</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#pricing">Pricing</a>
                    </li>
                    {# <li class="nav-item dropdown" aria-expanded="false">
                        <a class="nav-link dropdown-toggle" href="#" id="dropdown01" data-bs-toggle="dropdown" aria-expanded="false">Drop</a>
                        <ul class="dropdown-menu" aria-labelledby="dropdown01">
                            <li><a class="dropdown-item" href="article.html">Article Details</a></li>
                            <li><div class="dropdown-divider"></div></li>
                            <li><a class="dropdown-item" href="terms.html">Terms Conditions</a></li>
                            <li><div class="dropdown-divider"></div></li>
                            <li><a class="dropdown-item" href="privacy.html">Privacy Policy</a></li>
                        </ul>
                    </li> #}
                </ul>

                {% if is_granted('ROLE_ADMIN') %}
                <span class="nav-item">
                    <a class="btn-solid-sm" href="{{path('app_admin_home')}}">Dashboard</a>
                </span>
                {% endif %}
                {% if is_granted('ROLE_USER') %}
                <span class="nav-item">
                    <a class="btn-solid-sm" href="{{path('app_logout')}}">Se déconnecter</a>
                </span>
                {% else %}
                <span class="nav-item">
                    <a class="btn-solid-sm" href="{{path('app_login')}}">Se connecter</a>
                </span>
                {% endif %}
                
                
            </div> <!-- end of navbar-collapse -->
        </div> <!-- end of container -->
    </nav>